launguage: generic

sudo: required
services:
  - docker

before_install:
  - docker build -t andy/react -f Dockerfile.dev .

script:
  - docker run -e CI=true andy/react npm run test -- --coverage


